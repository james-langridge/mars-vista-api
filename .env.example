# Mars Vista API - Environment Configuration
#
# Copy this file to .env and update values for your environment.
# Never commit .env files with real credentials.

# =============================================================================
# DATABASE (Required)
# =============================================================================

# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://marsvista:your_secure_password@localhost:5432/marsvista

# Docker Compose PostgreSQL settings (used by docker-compose.yml)
POSTGRES_USER=marsvista
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=marsvista
POSTGRES_PORT=5432

# =============================================================================
# CACHE (Optional - falls back to memory if unavailable)
# =============================================================================

# Redis connection string
# Format: redis://[password@]host:port
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379

# =============================================================================
# AUTHENTICATION (Required for protected endpoints)
# =============================================================================

# Internal API secret for trusted service-to-service calls
# Used by frontend dashboard to manage API keys
# Generate with: openssl rand -base64 32
INTERNAL_API_SECRET=generate_a_secure_random_string_here

# Admin API key for scraper control endpoints
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=your_admin_api_key_here

# =============================================================================
# API CONFIGURATION
# =============================================================================

# API port (default: 5127 for development, 5000 for Docker)
API_PORT=5127

# Environment: Development, Production, or Test
ASPNETCORE_ENVIRONMENT=Development

# =============================================================================
# MONITORING (Optional)
# =============================================================================

# Sentry DSN for error tracking (leave empty to disable)
SENTRY_DSN=

# =============================================================================
# RAILWAY DEPLOYMENT (Production)
# =============================================================================
# When deploying to Railway, set these in the Railway dashboard:
#
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# REDIS_URL=${{Redis.REDIS_URL}}
# ASPNETCORE_ENVIRONMENT=Production
# INTERNAL_API_SECRET=<your-secret>
# ADMIN_API_KEY=<your-key>
# SENTRY_DSN=<your-dsn>
