# Local PostgreSQL Configuration (Docker)
POSTGRES_USER=marsvista
POSTGRES_PASSWORD=your_local_password
POSTGRES_DB=marsvista_dev
POSTGRES_PORT=5432

# Railway PostgreSQL Configuration (Production)
RAILWAY_HOST=your_railway_host
RAILWAY_PORT=your_railway_port
RAILWAY_USER=postgres
RAILWAY_PASSWORD=your_railway_password
RAILWAY_DB=railway

# Railway Connection URL
RAILWAY_DATABASE_URL=postgresql://user:password@host:port/database

# API Key (optional - protects API when set)
# Leave empty/unset to disable API key authentication
# When set, all endpoints (except /health) require X-API-Key header or ?api_key= parameter
API_KEY=your_secret_api_key_here
