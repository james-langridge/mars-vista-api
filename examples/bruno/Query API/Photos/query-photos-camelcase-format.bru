meta {
  name: Query Photos (camelCase format)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/rovers/curiosity/photos?sol=1&format=camelCase
  body: none
  auth: inherit
}

headers {
  X-API-Key: {{apiKey}}
}

params:query {
  sol: 1
  format: camelCase
}
