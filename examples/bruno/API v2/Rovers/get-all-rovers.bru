meta {
  name: Get All Rovers
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v2/rovers
  body: none
  auth: inherit
}

headers {
  X-API-Key: {{apiKey}}
}

docs {
  # Get All Rovers

  List all rovers with their details and statistics.

  ## Response includes:
  - Basic rover info (name, status, landing/launch dates)
  - Mission statistics (max_sol, max_date, total_photos)
  - Consistent JSON:API envelope format
}
