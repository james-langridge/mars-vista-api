name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: marsvista
          POSTGRES_PASSWORD: marsvista_dev_password
          POSTGRES_DB: marsvista_dev
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore MarsVista.sln

    - name: Build
      run: dotnet build MarsVista.sln --no-restore --configuration Release

    - name: Run API tests
      env:
        ASPNETCORE_ENVIRONMENT: Test
        ConnectionStrings__DefaultConnection: "Host=localhost;Port=5432;Database=marsvista_dev;Username=marsvista;Password=marsvista_dev_password"
      run: dotnet test tests/MarsVista.Api.Tests/MarsVista.Api.Tests.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=api-test-results.trx" --results-directory ./TestResults

    - name: Run Scraper tests
      run: dotnet test tests/MarsVista.Scraper.Tests/MarsVista.Scraper.Tests.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=scraper-test-results.trx" --results-directory ./TestResults

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: './TestResults/*.trx'
